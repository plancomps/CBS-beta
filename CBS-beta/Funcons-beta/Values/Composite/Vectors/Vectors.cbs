### Vectors

[
  Datatype vectors
  Funcon   vector
  Funcon   vector-elements
]

Meta-variables
  T <: values

Datatype
  vectors(T) ::= vector(V*:(T)*)

Funcon
  vector-elements(VV:vectors(T)) : =>(T)*
Rule
  vector-elements(vector(V*:(T)*)) ~> V*

// Consider defining n-vectors(N:naturals, T) as a dependent type. FIX-ME
