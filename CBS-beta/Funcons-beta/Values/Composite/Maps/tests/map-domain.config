general {
    funcon-term: 
      initialise-giving finalise-failing 
      sequential (
      print map-domain(map()),
      print map-domain(map(1,2)),
      print map-domain(map(1,2,3,4)),
      print map-domain(map(1,2,3,none))
      )
        ;
}

tests {
    result-term: none;
    standard-out: [{}, {1}, {1,3}, {1,3}];
}
