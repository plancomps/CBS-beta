general {
    funcon-term: 
      initialise-giving finalise-failing 
      sequential (
      print map-elements map(),
      print map-elements map(1,2),
      print map-elements map(3,4,5,6)
      )
        ;
}

tests {
    result-term: none;
    standard-out: [1, 2, 3, 4, 5, 6 ];
}
