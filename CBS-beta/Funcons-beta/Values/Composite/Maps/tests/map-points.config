general {
    funcon-term: 
      initialise-giving finalise-failing 
      sequential (
      print map-points map(),
      print map-points map(1,2),
      print map-points map(3,4,5,6)
      )
        ;
}

tests {
    result-term: none;
    standard-out: [tuple(1, 2), tuple(3, 4), tuple(5, 6) ];
}
