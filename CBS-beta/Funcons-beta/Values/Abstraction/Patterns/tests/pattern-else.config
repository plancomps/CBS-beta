general {
    funcon-term: 
      initialise-giving
      initialise-binding
      sequential(
        check-true is-equal(
          match(false,pattern-else(pattern-bind"a",pattern-bind"b")), {"a"|->false}),
        check-true is-equal(
          match(false,pattern-else(pattern-type ints,pattern-bind"b")), {"b"|->false}),
        else(
          effect match(false,pattern-else(pattern-type ints,pattern-type atoms)),
          print"OK"))
        ;
}

tests {
    result-term: none;
    standard-out: ["OK"];
}
