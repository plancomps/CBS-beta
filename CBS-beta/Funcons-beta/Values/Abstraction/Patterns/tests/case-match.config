general {
    funcon-term: 
      initialise-giving
      initialise-binding
      give({"a"|->false,"b"|->true},
        case-match(
          {"b"|->pattern-bind"d","a"|->pattern-bind"c"}, 
          bound-value"d"))
        ;
}

tests {
    result-term: true;
}
