general {
    funcon-term: 
      initialise-storing
      give(
        allocate-initialised-variable(bools,true),
        check-true structural-assigned(given))
        ;
}

tests {
    result-term: none;
}
