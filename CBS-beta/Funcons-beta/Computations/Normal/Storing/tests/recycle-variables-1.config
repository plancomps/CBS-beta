general {
    funcon-term: 
      initialise-storing
      give(
          allocate-variable(booleans),
          recycle-variables(given))
        ;
}

tests {
    result-term: null-value;
    store: map();
}
