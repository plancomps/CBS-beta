general {
    funcon-term: 
      initialise-storing
      give(
          allocate-variable(bools),
          recycle-variables(given))
        ;
}

tests {
    result-term: none;
    store: map();
}
