general {
    funcon-term: 
      initialise-storing
      give(
        allocate-initialised-variable(bools,false),
        sequential(
          assign(given,true),
          else(assign(given,42), print"OK")))
        ;
}

tests {
    result-term: none;
    standard-out: ["OK"];
}
