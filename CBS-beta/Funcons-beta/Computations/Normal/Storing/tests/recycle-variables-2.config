general {
    funcon-term: 
      initialise-storing
      give(
        allocate-variable(bools),
        recycle-variables(given,allocate-variable(bools)))
        ;
}

tests {
    result-term: none;
    store: map();
}
