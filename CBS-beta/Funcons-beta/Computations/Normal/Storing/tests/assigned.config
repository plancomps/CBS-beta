general {
    funcon-term: 
      initialise-storing
      give(
        allocate-initialised-variable(bools,true),
        sequential(
          check-true assigned(given),
          store-clear,
          else(assigned(given), print"OK")))
        ;
}

tests {
    result-term: none;
    standard-out: ["OK"];
}
