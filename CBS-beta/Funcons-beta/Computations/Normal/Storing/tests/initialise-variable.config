general {
    funcon-term: 
      initialise-storing
      give(
        give(
          allocate-variable(bools),
          seq(initialise-variable(given,true),given)),
        else(initialise-variable(given,true), print"OK"))
        ;
}

tests {
    result-term: none;
    standard-out: ["OK"];
}
