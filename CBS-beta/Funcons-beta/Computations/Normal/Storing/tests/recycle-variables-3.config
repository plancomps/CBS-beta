general {
    funcon-term: 
      initialise-storing
      give(
        allocate-variable(bools),
        sequential(
          recycle-variables(given),
          else(recycle-variables(given), print"OK")))
        ;
}

tests {
    result-term: none;
    standard-out: ["OK"];
}
