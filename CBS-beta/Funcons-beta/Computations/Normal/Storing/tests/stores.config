general {
    funcon-term:
      initialise-storing
      give(fresh-atom,
      and(
        is-in-type(map(),stores),
        is-in-type({given|->true},stores),
        is-in-type({given|->true, fresh-atom|->()},stores)))
        ;
}

tests {
    result-term: true;
}
