Language "Test"

# ASCII characters

/*
The apostrophe and the backslash have to be escaped in terminal symbols.
*/

Syntax
  test ::= '! " # $ % & \' ( ) * + , - . / : ;'
         | '< = > ? @ [ \\ ] ^ _ ` { | } ~ A Z a z 0 9'
