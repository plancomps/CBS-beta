general {
  funcon-term: sequential (
      check-true(is-value(false))
    , check-true(not is-value( ))
    , print "OK"
    );
}

tests {
    result-term : null-value;
    standard-out : ["OK"];
} 
