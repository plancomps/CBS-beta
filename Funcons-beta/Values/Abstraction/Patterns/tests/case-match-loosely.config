general {
    funcon-term: 
      initialise-giving
      initialise-binding
      give({"a"|->false,"b"|->true},
        case-match-loosely(
          {"b"|->pattern-bind"d"}, 
          bound-value"d"))
        ;
}

tests {
    result-term: true;
}
