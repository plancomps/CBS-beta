general {
    funcon-term: 
      and(
        is-in-type(map(),environments),
        is-in-type({"x"|->true},environments),
        is-in-type({"x"|->true, "y"|->false},environments))
        ;
}

tests {
    result-term: true;
}
