general {
    funcon-term: 
      initialise-storing
      give(
        allocate-initialised-variable(booleans,false),
        sequential(
          assign(given,true),
          else(assign(given,42), print"OK")))
        ;
}

tests {
    result-term: null-value;
    standard-out: ["OK"];
}
