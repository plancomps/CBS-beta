general {
    funcon-term: 
      initialise-storing
      sequential(
        effect allocate-variable(booleans),
        store-clear)
        ;
}

tests {
    result-term: null-value;
    store: map();
}
