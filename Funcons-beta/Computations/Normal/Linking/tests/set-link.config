general {
    funcon-term:  
      initialise-linking
      give(fresh-link(booleans),
        sequential(
          set-link(given, true),
          print"OK",
          else(
            set-link(given,false),
            print"OK")))
        ;
}

tests {
    result-term: null-value;
    standard-out: ["OK", "OK"];
}
